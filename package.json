{
  "name": "l2-mint-and-lock-bridge",
  "private": true,
  "workspaces": ["ui", "relayer", "contracts"],
  "packageManager": "bun@1.2.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "smoke:ws": "bun scripts/smoke-ws.ts"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}