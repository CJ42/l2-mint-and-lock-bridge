---
phase: 3
slug: flow-orchestration-ui-integration
status: approved
shadcn_initialized: false
preset: none
created: 2026-07-25
---

# Phase 3 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

This contract governs three new/changed surfaces in `ui/src/`: the hand-built `Stepper`
(`stepper.tsx`), the hand-built `ActionButton` (`action-button.tsx`), and the single error
banner replacing three scattered `<p className={styles.error}>` blocks in `bridge-card.tsx`
(lines 345–351). It does not touch `message-explorer.tsx` (Phase 2 territory) or the hero
title/subtitle/facts markup (unchanged, only extended with the stepper mount point).

Overrides in force, matching PROJECT.md / 03-CONTEXT.md precedent (recorded, not silent):
**shadcn/ui is explicitly not used** — the reference pattern in `TX_FLOW.md` (`Button`,
`Spinner` imported from `@/components/ui/*`) is reimplemented as hand-built CSS Module
components (`ActionButton`, `Spinner`) importing from `@/components/*` directly.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — shadcn explicitly rejected (PROJECT.md Key Decisions); `components.json` confirmed absent |
| Preset | not applicable |
| Component library | none — hand-built CSS Modules, co-located `component-name.module.css` |
| Icon library | none — hand-drawn inline SVG, 16×16 `viewBox`, `stroke="currentColor"`, `strokeWidth="1.4"`, `strokeLinecap`/`strokeLinejoin="round"`, `aria-hidden="true"` — matches the existing `SwapIcon()` in `bridge-card.tsx` exactly; new icons (wallet, retry, checkmark) follow the same convention |
| Font | IBM Plex Mono (`--font-plex`, already loaded via `next/font/google` with weights 400/500/600/700); this phase uses only 400 and 600 |

New shared component this phase must introduce: `ui/src/components/spinner.tsx` +
`spinner.module.css` — a single animated ring glyph (CSS `@keyframes` rotation, no JS), consumed
by both the stepper (blue-on-white) and the action button (white-on-blue). One component, two
color props — not two separate hand-rolled spinners.

---

## Spacing Scale

Declared values (must be multiples of 4) — governs the **new** elements this phase adds
(stepper, action-button internals, error banner). Pre-existing `bridge-card.tsx`/`page.tsx`
spacing (26px card padding, 18–22px field gaps, etc.) predates this phase and is not touched.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Step label-to-caption gap; badge internal padding-y |
| sm | 8px | Step circle-to-text gap; button icon/spinner-to-label gap; badge padding-x |
| md | 16px | Error banner padding; gap between stepper and hero facts block |
| lg | 24px | Vertical gap between stepper steps (row rhythm) |
| — | 24px | Step circle diameter (fixed across all three step states — see Stepper Visual States) |
| — | 48px | Action button min-height (existing `.action`, unchanged, already a multiple of 8) |

Exceptions: step circle diameter (24px) and connector line width (2px) are fixed decorative
constants, not spacing rhythm — declared here for completeness, not counted against the scale.
No 44px touch-target exception is needed: step circles are non-interactive (display only, no
click handler); the action button's existing 48px min-height already clears the touch-target bar.

---

## Typography

Declared for this phase's new elements only (existing H1/H2/body/label styles in
`page.module.css` / `bridge-card.module.css` are unchanged and out of scope).

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body (step caption, error banner body) | 11px (0.7rem) | 400 | 1.5 |
| Label (step title, button label, banner headline) | 13px (0.8rem) | 600 | 1.3 (button label: 1, single line, uppercase, 0.06em letter-spacing — reuses `.action`'s exact text treatment) |
| Micro (transport-health badge only) | 10px (0.62rem) | 600 | 1.3 — matches existing `.tag`/`.pending`/`.finalized` badge precedent in `message-explorer.module.css`, not a new invention |
| Heading / Display | not applicable | — | — | this phase introduces no new heading or display text; existing H1 (`page.module.css` `.title`) and H2 (`bridge-card.module.css` `.cardHeading h2`) are unchanged |

Two weights only, matching existing app-wide usage: **400** (regular — body/caption/banner text)
and **600** (semibold — labels, button text, badges). Weight 700 remains reserved for the H1/brand
mark, untouched by this phase.

---

## Color

The app already carries three pre-existing accent-adjacent colors (blue = primary interactive,
orange = brand mark + advisory notices, danger-red = errors) rather than a clean single 10%
accent — that is inherited, not introduced here. This phase adds exactly **one** new semantic
color, tightly scoped, because STEP-02 mandates a green checkmark that has no existing token:

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `--white` `#ffffff` | Page background, card background, input fields |
| Secondary (30%) | `--surface` `#fafafb` / `--border` `#e3e3e8` | Chain panels, card borders, upcoming-step circle border |
| Accent — primary (10%) | `--blue` `#181ea9` | CTA button fill, links, focus rings, pending-step spinner, `.tag`, `.self` button |
| Accent — brand/advisory (pre-existing) | `--orange` `#e5673f` | Title punctuation dot, "Bridge undeployed" notice, message-explorer "live" dot — **not extended by this phase** |
| **New — success (this phase)** | `--green` `#1f9254` (add `--green-soft: rgb(31 146 84 / 10%)`, `--green-line: rgb(31 146 84 / 24%)` to `globals.css`, mirroring the existing `--blue-soft`/`--blue-line` naming pattern) | **Reserved exclusively for confirmed/success signals: step-confirmed circle fill + checkmark, the "Done ✓ — view on explorer" button's `success` variant border/accent, and nothing else** — never used for any other interactive or informational element |
| Destructive | `--danger` `#c8102e` (add `--danger-soft: rgb(200 16 46 / 10%)` to `globals.css`, mirroring `--orange-soft`) | Error banner headline + left-border accent, field-level validation errors (existing `.error`) — never used for button chrome, even on the `failed` flow state (the button stays primary/blue; the banner alone carries the danger signal) |

Accent reserved for: **blue** — the action button in every non-terminal state, all links, focus
rings, the pending-step spinner and its circle border. **green** — step-confirmed circles/checks
and the completed-state button only. **orange** — unchanged pre-existing usage (title dot,
undeployed notice, explorer live dot), not touched or extended by this phase. **danger-red** —
error banner and field validation only, never the button.

**60/30/10 confirmation for this phase's new elements** (added per the checker's Dimension 3
recommendation — the split was previously stated as inherited but not re-declared for the surfaces
this phase introduces): across the stepper, action button, error banner, spinner and transport
pill, `--white` remains the dominant 60% (step circle fills in `upcoming` and `pending`, banner
background, button label text on blue); `--surface`/`--border` hold the secondary 30% (upcoming
circle borders, inactive connector lines, the polling-fallback pill background); and blue + green +
danger + orange **combined** stay within the 10% accent budget — enforced structurally by the
reserved-for lists above, since at most one accent is ever active per surface at a time (a step
circle is blue *or* green, never both; the button is blue *or* green, never danger).

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (idle state) | `Bridge` — one click drives both the approve and the bridge transaction (D-02); the label deliberately does not say "Approve" because the user should not have to think about the two-transaction mechanics, the stepper communicates that |
| Empty state | Not applicable in this phase's scope — no data-collection/list view is introduced here (the message explorer's empty state is Phase 2 territory, already shipped: `"Scanning both chains…"` / `"No bridge messages found."`) |
| Error state (banner) | Two-part: a short bold **headline** naming which stage failed (`Approval failed`, `Simulation failed`, `Bridge transaction failed`, `Transaction rejected`) in `--danger`, followed by the decoded reason in `--ink` from Phase 1's error-decode chain (e.g. `"BridgeMessageAlreadyProcessed — this message (messageId 0x1234…) was already relayed."`). When the decode chain returns a faucet link (ERR-02) or an explorer link, it renders inline as an underlined `--blue` link within the body sentence, never as a separate CTA |
| Destructive confirmation | None in this phase. Flipping direction after a completed run clears the stepper's visible checkmarks (D-08) but the underlying message stays permanently visible in the message explorer, so no confirmation dialog is needed — nothing is actually destroyed |

### Action button — full state → copy table (BTN-01, BTN-02)

Derived **entirely** from the single `BridgeFlowState` union (D-06); no second derivation. Exact
TypeScript variant names/sub-discriminators are the plan's discretion per 03-CONTEXT.md D-06 —
this table is the required visual/copy OUTPUT for each named moment regardless of encoding.

| Flow state | Label | Icon (idle) | Spinner? | Disabled | Variant |
|---|---|---|---|---|---|
| `disconnected` | `Connect wallet` | wallet outline | no | no | primary |
| `wrong-network` (idle) | `Switch network` | swap/retry glyph | no | no | primary |
| `wrong-network` (switching) | `Switching network…` | — | yes | yes | primary |
| `undeployed` | `Bridge undeployed` | none | no | yes | primary (uses existing `.action:disabled` styling — no new CSS) |
| `incomplete-input` (no amount) | `Enter an amount` | none | no | yes | primary (disabled styling) |
| `incomplete-input` (no recipient) | `Enter a recipient` | none | no | yes | primary (disabled styling) |
| `idle` | `Bridge` | arrow/transfer glyph | no | no | primary |
| `approving` (awaiting signature) | `Confirm approval in wallet` | — | yes | yes | primary |
| `approving` (confirming) | `Approving…` | — | yes | yes | primary |
| `submitting` (awaiting signature) | `Confirm in wallet` | — | yes | yes | primary |
| `submitting` (confirming) | `Submitting…` | — | yes | yes | primary — **not** `Locking…`; the existing copy was collateral-direction-specific and wrong for the burn direction, corrected here |
| `relaying` | `Relaying…` | — | yes | yes | primary |
| `done` | `Done ✓ — view on explorer` (renders as an anchor, unchanged behavior) | checkmark glyph | no | no | **success** (new `--green` variant — border/text accent, matches the stepper's confirmed color) |
| `failed` | `Try again` | retry glyph | no | no | primary — **not** danger-colored; the banner alone carries the failure signal, per PROJECT.md's "no per-step failed state" rule extended to the button |

BTN-02 compliance: for every row where "Spinner?" = yes, the `Spinner` component (blue-on-white
elsewhere, **white-on-blue** here since the button fill is blue) fully replaces the icon slot —
never rendered alongside an icon — and `disabled` is `true` for the entire pending/processing
window, re-enabling only on the next terminal state (`done`, `failed`, or a fresh `idle`).

---

## Stepper — Visual States (STEP-01 / STEP-02 / STEP-03 / STEP-04 / D-07 / FLOW-05)

Three steps, always visible (D-07), fixed order: **Bridge Approved → Bridge tx submitted →
Bridge tx relayed**. Mounted in `page.tsx`'s `styles.hero` column, directly after `.facts`
(STEP-01), with `margin-top: 16px` relative to the facts list.

### The three per-step states

Every step circle is a **fixed 24px diameter** in all three states — this is a hard constraint,
not a suggestion: STEP-02 only names two states (pending/confirmed) but D-07 requires an
always-visible stepper with **no layout shift** as steps advance, so the third state (`upcoming`,
introduced by this contract) must occupy identical space.

| State | Circle | Glyph | Label color | Caption shown? | Caption color |
|---|---|---|---|---|---|
| **upcoming** (new — this contract's answer to the open question) | 24px, `1.5px solid var(--border)`, background `var(--white)` | step index digit (`1`/`2`/`3`), 11px/600, `var(--muted)` | `var(--muted)` | **no** — showing nothing is the honest choice; inventing placeholder copy (e.g. "Not started") would be the first faked-state STEP-03 does not ask for | — |
| **pending** (STEP-02 blue spinner) | 24px, `1.5px solid var(--blue-line)`, background `var(--white)` | `<Spinner>` (blue-on-white, 16px) | `var(--ink)` | yes | `var(--muted)`, 11px |
| **confirmed** (STEP-02 green checkmark) | 24px, filled `var(--green)`, no border | white checkmark glyph, 12px | `var(--ink)` | yes | `var(--muted)`, 11px |

Connector line between two circles: 2px wide, `var(--green)` if the step **above** it is
confirmed, else `var(--border)`. This is a real-state trail, not a progress bar — it only ever
reflects a transition that has actually completed (no faked interpolation), consistent with the
no-faked-states constraint.

### STEP-03 copy mapping — which of the three TX_FLOW.md sentences each step shows

TX_FLOW.md defines exactly three sentences (pending / processing / confirmed), written for a
single wallet-driven transaction with two distinct in-flight phases. Steps 1 and 2 are each one
wallet transaction and map cleanly to all three; step 3 has no wallet interaction and only one
in-flight phase, so it uses one of the two "in-flight" sentences, not both — this is the
resolution the additional-context flagged as needed.

| Step | Pending caption (wallet prompt open) | Processing caption (receipt awaited) | Confirmed caption |
|---|---|---|---|
| 1. Bridge Approved | *"Your transaction is being submitted to the network…"* | *"Your transaction has been picked up and is being processed…"* | *"Your transaction has completed successfully!"* |
| 2. Bridge tx submitted | *"Your transaction is being submitted to the network…"* (also covers the brief D-01 simulate-then-open-wallet gap between step 1 confirming and this wallet prompt opening — no separate "simulating" copy is invented) | *"Your transaction has been picked up and is being processed…"* | *"Your transaction has completed successfully!"* |
| 3. Bridge tx relayed | — (no wallet interaction) | *"Your transaction has been picked up and is being processed…"* — the **only** caption this step ever shows while active, for the entire relay-watch window (there is no separate "processing" sub-phase to distinguish it from) | *"Your transaction has completed successfully!"* |

### Failure presentation (FLOW-05 — the subtlest question in this phase)

PROJECT.md rules out a per-step failed state (V2-01, deferred) and mandates one shared error
banner. The rendering rule that satisfies both FLOW-05 ("leaves pending, nothing left spinning")
and that constraint simultaneously:

**On `failed`, any step whose derived per-step status is not literally `confirmed` renders as
`upcoming` — reverting from `pending` back to its neutral, honest starting state. Steps already
`confirmed` at the moment of failure keep their green checkmark (that transaction really did
succeed — reverting it would be dishonest, not neutral).** No step ever renders red, gets a
special "failed" glyph, or gets a shaking/error animation. 100% of the failure explanation lives
in the error banner; the stepper's only job during a failure is to stop claiming something is
still in flight.

Example: bridge write reverts after approve already confirmed → step 1 stays green-checked, step
2 reverts from its spinner to the neutral `upcoming` circle, step 3 stays `upcoming` (it was
already there) — and the error banner explains why, with the button offering `Try again`.

### Refresh-recovery rendering (FLOW-04, visual half only — persistence mechanics are the hook's concern)

On mount, before rehydration resolves, the stepper renders in its `upcoming` state for all three
steps — this is not a separate loading skeleton, `upcoming` already IS the correct "we don't know
yet" visual (it claims nothing). Once rehydration resolves (from persisted storage, backstopped
by a bounded chain scan per Pitfall 7), the stepper repaints once to the real restored state. No
intermediate "restoring…" spinner state is introduced — that would be a state STEP-02/03 never
defined and risks becoming a fourth faked phase.

---

## Transport Health Visibility (Phase 2 D-07's deferred call — resolved here)

The tension on record: the milestone's core value is "never left guessing," but an always-on
connection indicator is chrome noise on a portfolio demo. Resolution — **render only when
degraded, and only where it is load-bearing**:

- **Scope:** this contract governs only the flow's own stepper (step 3, "Bridge tx relayed" —
  the one step actually driven by `use-relay-status.ts`'s live watch). The message explorer's own
  live/staleness indicator is Phase 2/LIVE-09 territory and is not extended by this phase.
- **When `transportMode === 'connected'` (or the value has not yet resolved on first mount):
  render nothing.** Absence is the signal for "healthy" — this is deliberate, not an oversight:
  showing a permanent "connected" badge is exactly the chrome-noise outcome CONTEXT.md's tension
  note warns against, and treating "not yet resolved" the same as "connected" avoids a
  first-paint flash of a false "reconnecting" badge before the watchdog has classified anything.
- **When `transportMode` is `stale` or `reconnecting`: show a small pill directly under step 3's
  caption** (only while step 3 is the active/pending step — a user is only watching for relay
  liveness during that window): `Reconnecting…`, `--orange` text on `--orange-soft` background,
  10px/600, `4px 8px` padding, matching the existing `.tag`/`.pending` badge treatment.
- **When `transportMode === 'polling-fallback'`: show the same pill in a calmer tone** —
  `Live updates via polling`, `--muted` text on `--surface` background — because polling-fallback
  is a working degraded mode, not an alarm; it should read as "still honest," not "something is
  wrong."
- This satisfies the core-value tension without new chrome: the ONE moment transport health
  actually matters to the user (waiting on the relay step) is the only moment it can appear.

---

## UI Considerations

Produced by the post-verification UI-consideration probe (`ui-consideration-probe.cjs`) over five
described surfaces. **27 applicable considerations raised: 21 covered, 6 dismissed with reason,
0 unresolved**, plus 1 carried-forward backstop outside the raised set (recorded below rather than
dropped).

Detected element kinds (classifier output, confirmed — no kind was added): E1 Stepper
`list-collection` + `static-content`; E2 ActionButton `form` + `media` + `interactive-control` +
`static-content`; E3 Error banner `list-collection` + `static-content`; E4 Spinner
`interactive-control` + `static-content`; E5 Transport pill `static-content`.

### E1 — Stepper (`stepper.tsx`)

| Category | Status | Statement / Reason |
|----------|--------|--------------------|
| empty | covered | Before any flow starts, all three steps render in the `upcoming` state: a 24px circle with `1.5px solid var(--border)`, the step index digit in `var(--muted)`, and no caption |
| loading | covered | A pending step renders the blue `<Spinner>` inside its 24px circle with its caption shown; on initial mount before rehydration resolves, all three steps render as `upcoming` rather than a separate skeleton |
| error | covered | On `failed`, every step whose per-step status is not literally `confirmed` reverts to the `upcoming` circle; steps already `confirmed` keep their green checkmark; no step renders red or gains a failed glyph |
| populated | covered | The all-confirmed state renders three green-filled circles with white checkmarks and green connector lines, alongside the `success`-variant `Done ✓ — view on explorer` button |
| partial | covered | A mixed stepper is first-class, not an edge case: step 1 `confirmed` green while steps 2 and 3 are `upcoming` is the exact rendering required after a bridge write reverts post-approval |
| overflow | covered | Step captions wrap freely to as many lines as needed with no truncation and no ellipsis; the step row grows vertically and the 2px connector line stretches to match |
| zero-one-many | dismissed | **Reason:** the stepper has fixed arity — always exactly three steps, never zero and never variable. No singular/plural copy or count-dependent spacing exists to resolve |
| long-text | covered | Captions are three fixed strings from `TX_FLOW.md`; the longest (*"Your transaction has been picked up and is being processed…"*) wraps under the overflow rule above and is never clipped |

### E2 — ActionButton (`action-button.tsx`)

| Category | Status | Statement / Reason |
|----------|--------|--------------------|
| empty | dismissed | **Reason:** all fourteen rows of the flow-state → copy table declare a non-empty label. There is no state in which the button renders without text, so an empty rendering cannot occur |
| loading | covered | For every state where the copy table marks Spinner = yes, the `<Spinner>` (white-on-blue) fully replaces the icon slot — never rendered alongside an icon — and `disabled` stays `true` for the entire pending/processing window |
| error | covered | The `failed` state renders `Try again` with a retry glyph in the unchanged primary/blue variant; danger colour never reaches the button, the banner alone carries the failure signal |
| populated | covered | `idle` renders `Bridge` with the arrow/transfer glyph; `done` renders as a `success`-variant anchor reading `Done ✓ — view on explorer` |
| partial | covered | The `incomplete-input` states are the partial-input rendering: `Enter an amount` and `Enter a recipient`, both disabled with no icon |
| overflow | covered | The button stays single-line at 48px min-height in every state; the existing `0.06em` letter-spacing tightens on the longest labels so they fit the card width. No wrapping, no ellipsis, no height change between states |
| long-text | covered | `Confirm approval in wallet` (26 characters) is the longest declared label and sets the letter-spacing tightening bound; all other labels render at the untightened default |

### E3 — Error banner (in `bridge-card.tsx`)

| Category | Status | Statement / Reason |
|----------|--------|--------------------|
| empty | covered | The banner does not render at all when there is no error — absence is the no-error state, and no empty-banner shell is ever painted |
| loading | dismissed | **Reason:** the banner renders only on a terminal failure. It has no in-flight state of its own — the flow's loading states belong to the stepper and the button |
| error | covered | Two-part: a bold `--danger` headline naming the failed stage (`Approval failed`, `Simulation failed`, `Bridge transaction failed`, `Transaction rejected`) followed by the decoded reason in `--ink` from Phase 1's decode chain |
| populated | covered | Headline plus decoded sentence carrying real evidence values, with any faucet or explorer link rendered inline as an underlined `--blue` link inside the body sentence rather than as a separate CTA |
| partial | covered | When the decode chain yields no usable reason — ERR-06 (out-of-gas, empty revert data) or ERR-08 (unrecognised revert) — the body falls back to the raw hex revert data as selectable monospace, or `no revert data returned` for the out-of-gas case, plus the explorer link. The banner is never content-free |
| overflow | covered | Banner width matches the card; body text uses `overflow-wrap: anywhere` (existing `.chainBalance` precedent) so nothing is clipped or truncated |
| zero-one-many | dismissed | **Reason:** exactly one banner is ever shown. A new failure replaces the previous banner rather than stacking, so there is no multi-item layout to resolve |
| long-text | covered | Hex evidence — a 66-character `messageId`, raw calldata — wraps mid-token via `overflow-wrap: anywhere` and remains fully selectable for copying |

### E4 — Spinner (`spinner.tsx`)

| Category | Status | Statement / Reason |
|----------|--------|--------------------|
| overflow | dismissed | **Reason:** a fixed 16px glyph with no container-relative content. It cannot exceed its bounds because its size is a constant, not a function of content |
| long-text | dismissed | **Reason:** the spinner carries no text at all — it is a pure CSS-keyframe ring with `aria-hidden="true"` |

### E5 — Transport-health pill

| Category | Status | Statement / Reason |
|----------|--------|--------------------|
| overflow | covered | Two fixed strings on a single line at `4px 8px` padding, sitting under step 3's caption inside the hero column width — neither string approaches the available width |
| long-text | covered | `Live updates via polling` (24 characters at 10px/600) is the longest string; bounded by construction, so no wrapping or truncation rule is needed |

### Backstop (carried forward from the pre-probe draft — not raised by the probe, recorded rather than dropped)

- `{ statement: "An unresolved transportMode on first mount renders identically to 'connected' — that is, renders nothing — until the staleness watchdog classifies it, so no false 'Reconnecting…' pill flashes on first paint", verification: backstop }`

  Held out as a visual-state test rather than marked covered because it depends on Phase 2's hook
  timing (`use-relay-status.ts`'s watchdog classification latency), not purely on this phase's
  markup. At verify time this routes to `insufficient_spec → human_needed` unless a wired test
  provides explicit evidence.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|--------------|
| shadcn official | none — shadcn not initialized | not applicable |
| third-party | none | not applicable |

---

## Checker Sign-Off

Verified by `gsd-ui-checker` on 2026-07-25.

- [x] Dimension 1 Copywriting: **PASS** — all CTAs specific and actionable; error copy carries a solution path; no generic placeholder labels
- [x] Dimension 2 Visuals: **PASS** — focal point (hero column) and hierarchy declared; icons specified with `aria-hidden` and stroke conventions
- [x] Dimension 3 Color: **PASS** *(was FLAG — resolved)* — accent reserved-for lists prevent overuse; the 60/30/10 split has since been explicitly re-declared for this phase's new elements (see Color section)
- [x] Dimension 4 Typography: **PASS** — 3 sizes (10/11/13px), 2 weights (400/600), all line heights declared
- [x] Dimension 5 Spacing: **PASS** — all values multiples of 4 and within the standard set; the 2px connector and 24px circle justified as decorative constants
- [x] Dimension 6 Registry Safety: **PASS** — no third-party registries; shadcn confirmed absent (`components.json` not present)

**Approval:** approved — 6/6 dimensions pass, 0 blocking issues. The single non-blocking Dimension 3
recommendation was applied rather than accepted as a standing FLAG.
